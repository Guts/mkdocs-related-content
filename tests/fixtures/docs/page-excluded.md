---
title: Page excluded from scoring
tags:
  - api
related_content:
  exclude_from_scoring: true
---

# Page excluded from scoring

Shares the `api` tag with `page-a.md`, but opts itself out via its own
frontmatter - unlike `hide: [related_content]`, this must make it
disappear from the index entirely: never a candidate for any other page,
never given related pages of its own either.
