---
title: Page with related content hidden
tags:
  - api
hide:
  - related_content
---

# Page with related content hidden

Shares the `api` tag with `page-a.md`, so it must still be *computed* as
related content for other pages - only its own display is hidden via the
`related.html` partial's `hide` check.
